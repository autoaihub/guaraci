# Guaraci Core Dependencies
# Optional extras (install via: pip install "guaraci[<extra>]"):
#   datasus -> pyreaddbc/dbfread · api -> fastapi/uvicorn/httpx/aiofiles
#   snis-legacy -> google-cloud-bigquery/db-dtypes (legacy SNIS BigQuery path only)

polars>=0.20.0
pyarrow>=14.0.0
pandas>=1.5.0
tqdm>=4.64.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
click>=8.1.0
rich>=13.7.0
loguru>=0.6.0,<0.7.0
