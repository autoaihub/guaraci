# Handoff — 2026-05-29 (sessão autônoma: fontes climáticas NASA)

Autor: agente autônomo (Claude). Objetivo da sessão: integrar dados climáticos da
NASA ao Guaraci — NASA POWER primeiro, depois FIRMS e GPM IMERG —, em modo autônomo,
sem entregar o controle entre tarefas.

> **Leia primeiro a seção "ALERTA: estado do git / branches".**

---

## ALERTA: estado do git / branches

Durante esta sessão, **outro agente estava commitando na mesma branch
`feat/datasus-ftp-direto` e no mesmo diretório de trabalho**:

- No início da sessão o HEAD era `6151577` (DATASUS phase 4).
- Ao longo da sessão surgiram `7d38753` (phase 5A) e `ea97228` (phase 5B) — trabalho
  do agente de DATASUS FTP. Isso explica a suíte ter crescido de 319 para 364 testes
  sem que eu tenha adicionado esses testes.

Para **não misturar** a feature NASA com o trabalho de DATASUS FTP em andamento,
criei uma branch dedicada:

- **Branch nova: `feat/nasa-clima`** (criada a partir de `ea97228`).
- O diretório de trabalho `C:\Users\luisv\Documents\DS\guaraci` **agora está em
  `feat/nasa-clima`**. Se o agente de DATASUS (ou você) for retomar a phase 5C/6,
  rode antes: `git checkout feat/datasus-ftp-direto`.
- `feat/datasus-ftp-direto` ficou intacta em `ea97228`.
- Branch from `main` não era viável: minhas edições em `downloads.py` assentam sobre
  as mudanças de phase 5 (mais fontes registradas), então só fazem sentido a partir do
  HEAD atual. Quando o DATASUS FTP entrar na `main`, a `feat/nasa-clima` rebase limpo.

**Para revisar de manhã:** decidir se `feat/nasa-clima` vira PR próprio (recomendado)
ou se deve ser rebaseada/movida depois que o DATASUS FTP for mergeado.

---

## Tarefa 1 — Fonte NASA POWER (`nasa_power`)  ✅ implementada, testada, commitada

**O que fiz e por quê.** Integrei a NASA POWER API (Prediction Of Worldwide Energy
Resources) como nova fonte primária de séries climáticas/meteorológicas. É a fonte
oficial direta (NASA serve os dados; sem intermediário curador), aderente ao
Princípio 20 do vogel-stack. Cobre o Brasil inteiro (grade global ~0.5°×0.625°),
não exige autenticação e responde JSON — encaixe natural no padrão das fontes de API
do Guaraci.

**Arquitetura (espelha `guaraci/opendatasus/`):**
- `guaraci/nasa/__init__.py` — exporta `NasaPowerClient`, `NasaPowerClientError`,
  `NasaPowerDataSource`.
- `guaraci/nasa/client.py` — `NasaPowerClient` sobre `urllib` (stdlib, sem dep nova),
  com a mesma taxonomia de erro do OpenDataSUS (`category`/`retryable`/`hint`,
  `with_context`). Método `temporal_point(temporal, parameters, lat, lon, start, end,
  community)`.
- `guaraci/nasa/power.py` — `NasaPowerDataSource(DataSource)`: valida entradas, chama
  o client, parseia `properties.parameter.<VAR>.<período>` numa tabela larga (1 linha
  por período, 1 coluna por variável) + colunas derivadas `period/date/year/month/day`
  e `latitude/longitude/elevation`. Exporta csv/parquet/sqlite, escreve `manifest.json`
  (`DownloadManifest`) e emite eventos de progresso compatíveis com `jobs.py`.
- `guaraci/services/downloads.py` — adapter fino `NasaDownloadSource` + registro da
  fonte `nasa_power` (mode `"nasa power api"`) + `_normalize_nasa_power_params`.

**Decisões tomadas + justificativa:**
1. **Pacote próprio `guaraci/nasa/`** em vez de reusar OpenDataSUS: a NASA é outro
   publisher, com produtos heterogêneos (POWER/FIRMS/GPM). Mantém coesão e isola risco.
2. **Client stdlib, duplicando um pouco da plumbing HTTP do OpenDataSUS** em vez de
   extrair uma base compartilhada: extrair base mexeria em `opendatasus/client.py`
   (fonte que já funciona) → risco. A duplicação é isolada e reversível. *Futuro:*
   extrair `BaseHttpJsonClient` quando houver 3+ clients.
3. **`latitude`/`longitude` como entrada nativa** (tipo `string`, parse→float no
   datasource, pois o sistema de tipos do schema não tem float). Mapear município→
   centroide ficou como trabalho futuro (exige dataset de coordenadas do IBGE, que é
   outra integração de fonte primária). Escolha conservadora = expor o contrato nativo
   da API agora, conveniência depois.
4. **Lista curada de `parameters`** (14 variáveis) como `allowed_values`, em vez de
   aceitar o catálogo POWER inteiro (centenas). Dá dropdown discoverable na UI
   schema-driven e evita erros. **Validei as 14 ao vivo** contra o endpoint daily/AG
   (todas retornaram). Power users que precisem de variáveis exóticas: trabalho futuro
   (ex.: parâmetro livre + validação contra catálogo POWER).
5. **`temporal` daily + monthly.** O agregado anual mensal do POWER (`YYYY13`) é
   preservado sem perda como `month=13` (sem `date`) — filtrar `month <= 12` isola os
   meses verdadeiros. Documentado.
6. **`fill_value` lido de `header.fill_value`** (não hardcode -999) e convertido para
   null. Confirmei via probe ao vivo que o sentinela vive nesse campo.
7. **`output_format` default `None`** + warning quando não há artefato (igual
   OpenDataSUS/PySUS), por consistência de contrato, mesmo que um run default não gere
   arquivo. `keep_raw` salva o JSON bruto.
8. **Branch dedicada** (ver alerta acima).

**Como verifiquei (gates):**
- Probes ao vivo (GETs pequenos, sem auth) confirmaram o shape JSON daily e monthly e
  a validade das 14 variáveis — antes de escrever o parser.
- **Suíte completa: `2 failed, 404 passed, 3 skipped`.** Os 2 `failed` são
  **pré-existentes** em `tests/test_sinan_datasource.py` (`_download_file_safe` /
  single-worker, símbolos PySUS já removidos antes desta sessão — documentado no
  CHANGELOG da migração FTP). 3 skipped = smoke FTP opt-in + 1 Docker-specific. Nenhuma
  regressão (failures e skips idênticos ao baseline; +39 testes NASA + 1 de schema na
  API passam).
- 39 testes novos nos 3 arquivos NASA cobrindo: client (validação, taxonomia de erro,
  caminhos success/HTTP/URL/timeout/non-JSON via `urlopen` monkeypatchado), datasource
  (parse daily/monthly, fill→null, month=13, export csv/parquet/sqlite round-trip,
  keep_raw, manifest, warnings, contrato de eventos de progresso, 8 caminhos de
  validação), service (registro, schema, validação, run e2e com client monkeypatchado).

**Gates de lint/format/typecheck — calibração honesta:** o repo **não** mantém o
código limpo contra black/isort/flake8/mypy (todos falham em arquivos já commitados;
não há config de flake8 alinhada ao black de 88 colunas). Portanto o gate real do
projeto é a **suíte pytest** + smoke de import. Escrevi o código NASA tipado e no estilo
do `opendatasus/datasource.py` (que é tipado), mas **não reformatei arquivos
existentes** (seria diff enorme e fora de escopo, violando "mudanças pequenas e
isoladas"). Ver "O que revisar".

**Commit:** ver `git log feat/nasa-clima`.

---

## O que foi pulado/bloqueado

- **Validação operacional 1 download real via jobs/UND/Docker:** não rodei o servidor
  nem Docker (o prompt pede não travar a sessão com processos; preferi testes
  automatizados + probes ao vivo). O caminho jobs/progress está coberto por teste de
  contrato de eventos, não por um job assíncrono real.
- **Mapeamento município→lat/lon:** trabalho futuro (precisa de coordenadas IBGE).

## O que eu faria a seguir (e estou seguindo, se a sessão continuar)

- **FIRMS** (focos de incêndio NASA, near-real-time): precisa de `MAP_KEY` gratuito —
  implemento + testo com mocks; coleta ao vivo fica bloqueada na chave (documento).
- **GPM IMERG** (precipitação ~10 km): precisa de login NASA Earthdata e parsing
  NetCDF/HDF5 (dependência pesada). Avaliar se vale uma dep nova ou um caminho mais
  leve; pode ficar como stub documentado se o custo arquitetural for alto.

## O que você precisa revisar/confirmar

1. **Branch:** aprovar `feat/nasa-clima` como PR próprio (recomendado) ou pedir
   rebase/realocação após o merge do DATASUS FTP. E lembrar que o working dir está
   nessa branch (checkout de volta para `feat/datasus-ftp-direto` se for retomar DATASUS).
2. **Lista curada de `parameters`:** confirmar se as 14 variáveis cobrem seus casos de
   uso (dengue×chuva, internações×temperatura etc.) ou se quer o catálogo POWER completo.
3. **Contrato de entrada lat/lon:** confirmar se aceitar lat/lon cru está ok por ora,
   ou se o mapeamento por município deve subir de prioridade.
4. **Push:** se quiser a branch no remoto (ver nota no fim sobre status do push).
